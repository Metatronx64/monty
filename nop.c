#include "monty.h"

/**
 * nop - Does nothing.
 * @stack: Pointer to the stack (unused).
 * @line_count: Line count (unused).
 *
 * Return: Void.
 */
void nop(stack_t **stack, unsigned int line_count)
{
	(void)stack;  // Silence unused parameter warning.
	(void)line_count;  // Silence unused parameter warning.
}

